language:
    - python
cache:
    - pip
python:
    - "3.7"
install:
    - "pip install --upgrade -r requirements.txt"
script:
    - bash run_tests.sh
after_success:
    - coveralls

matrix:
    include:
        - os: linux
          dist: xenial
          sudo: true
